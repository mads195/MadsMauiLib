<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Mads195.MadsMauiLib.Controls.Card">
    <Border HorizontalOptions="Fill" StrokeThickness="0" BackgroundColor="White" Padding="0,10" Margin="20,20,20,12">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="15" />
        </Border.StrokeShape>

        <VerticalStackLayout Spacing="10">
            <Grid Padding="20,0">
                <Label Text="This is our card" />
            </Grid>
            <Grid HeightRequest="1" BackgroundColor="LightGray" Margin="0,5" />
            <Grid Padding="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" HorizontalOptions="Center">
                    <Label Text="Unlock" />
                </Grid>
                <Grid Grid.Column="1" HorizontalOptions="Center">
                    <Label Text="View" />
                </Grid>
                <Grid Grid.Column="2" HorizontalOptions="Center">
                    <Label Text="Delete" />
                </Grid>
            </Grid>
        </VerticalStackLayout>
    </Border>
</ContentView>
